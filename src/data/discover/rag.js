const project = {
  slug: 'rag',
  date: '2026-08-05 10:00',
  name: 'RAG from Scratch: Bolt-On Memory for LLMs',
  url: 'https://arxiv.org/abs/2005.11401',
  url2: 'https://arxiv.org/abs/2312.10997',
  description: 'RAG 的完整工作逻辑、分块这个最容易被低估的细节、怎么客观评估一个 RAG 好不好(RAGAS / RAGChecker / 中文 benchmark),以及从 Naive 到 Agentic RAG 的进化。',
  tags: ['RAG'],
  author: 'Shannon',
  detail:
`这个系列前几篇在聊 Agent:怎么约束 Agent 的行为(Superpowers)、怎么用图把流程定死(LangGraph)、怎么让 Agent 用工具(MCP)。这一篇换一个层——知识层:怎么让模型知道它没学过的东西。

核心问题一句话:**LLM 很强,但它的知识被冻结在训练截止那天。** 模型不会告诉你它不知道,它会一本正经地编。RAG(Retrieval-Augmented Generation,检索增强生成)的思路很朴素:别让模型猜,让它去查。答问题之前,先到你的资料库里把相关的几段捞出来,拼进 prompt,再让它基于这些资料回答。

这篇文章从工作逻辑讲起,拆到分块这种细节,讲怎么客观评估它好不好,看 RAG 从 2020 到现在的进化,最后照方抓药实测一个。重点放在分块——这个最容易被跳过、却几乎决定检索质量的环节。

## 工作逻辑:先检索,再生成

RAG 的原理一句话就能说完,但拆开是两条流水线,一条离线、一条在线。

**离线那条叫入库(indexing)**,把资料变成可检索的形式,只做一次:

1. **加载**——把 PDF、Markdown、网页、数据库记录读成纯文本。
2. **分块(chunking)**——把长文本切成一段段。全流程里最容易被低估的一步,后面单独讲。
3. **嵌入(embedding)**——每段文本用一个嵌入模型算出一个向量(几百到几千维的浮点数数组)。语义相近的文本,向量在空间里就离得近。
4. **存向量库**——向量和原文一起存进向量数据库。

**在线那条叫问答(inference)**,每次提问跑一遍:

1. 把问题用**同一个**嵌入模型算成向量。
2. 在向量库里做 **top-k 检索**——找跟问题向量最像的 k 段(通常 3～10)。
3. 把这几段拼进 prompt,让 LLM"基于以下资料回答"。
4. LLM 生成答案——答案的每个事实,理论上都能回溯到检索到的段落。

这个循环的关键在最后两步之间:LLM 自己不用"记住"任何资料,它只需要"读"检索结果。资料更新了,重新入库就行,模型权重不用动——这就是 RAG 对"知识过期"问题的天然解。

## 三个概念打底:嵌入、相似度、向量库

**嵌入(Embedding)** 是把文本变成向量的神经网络。它学到的是"语义坐标":说"退款"和"退货政策"的文本,即便没有共同词汇,向量也靠得近。向量的每一维都不可解释,意义活在相对位置里。中文语料里,本地开源常用 **bge-m3**(1024 维,MIT 协议),轻量演示用 nomic-embed,走 API 用 OpenAI 的 text-embedding-3。嵌入模型的质量通常比分块策略影响更大——先修分块,再谈换模型。

**相似度** 默认用余弦相似度(cosine)——两个归一化向量夹角的余弦,越接近 1 越像。它只看方向、不看长度,对文本向量最合适。注意一个铁律:**入库和查询必须用同一个嵌入模型、同一个度量**。混用会让索引在数学上失效,结果全部作废。

**向量库** 存储向量并支持快速找最近邻。先说度量:文本向量最常用余弦相似度,因为它只看方向、不看长度。实现上有个惯用招——入库前先把向量做 L2 归一化,归一化之后内积就等于余弦,所以 FAISS 里用 IndexFlatIP(内积索引)即可,后面实测 demo 里 encode 的 normalize_embeddings=True 就是这个用意。欧氏距离看的是长度,对文本向量没意义,一般不用。铁律跟嵌入模型那条一样:**入库和查询必须用同一个度量**,混用结果直接作废。

但无论用什么度量,精确找最近邻都是 O(n) 的暴力扫描——n 是向量总数,十万、百万、千万,一次查询扫全库,谁都扛不住。向量库的秘密武器是近似最近邻(ANN)索引:拿一点点召回损失,把复杂度从 O(n) 压到近对数级。主流三种:

- **HNSW**:一张多层"可导航小世界"图,顶层稀疏、底层稠密,查询从顶层贪心跳到目标,接近对数级复杂度。生产标配。
- **IVF(倒排文件)**:先对全库做 k-means 聚类,查询只搜最近的几个簇再线性扫,适合超大库,召回略低。
- **PQ(乘积量化)**:把向量压缩成短码(可到原来的 1/4～1/32),省内存省带宽,牺牲精度。

还要分清一件常被搞混的事:FAISS 是库,不是数据库。FAISS 是 Meta 的相似度搜索库,纯内存态,不管持久化,不管按元数据过滤,不管副本和权限。Chroma、Qdrant、Pinecone 才是数据库:持久化、按元数据过滤、横向扩展全内置。选型大致是:

| 场景 | 选什么 |
|---|---|
| 原型快跑 | Chroma |
| 规模 + 过滤 | Qdrant |
| 已有 Postgres | pgvector(加一列向量 + HNSW 索引) |
| 托管省事 | Pinecone |
| 自己搭检索层 | FAISS |

生产里元数据过滤不是加分项,是必需项:通常先按权限/日期/租户把候选集缩到子集,再在子集里做 ANN——检索的正确性和性能都靠这一下。

顺便说一句天花板:2025 年 DeepMind 的分析指出,单向量稠密嵌入有一个可证明的召回上限——512 维在约 50 万篇文档、1024 维在约 400 万篇时开始漏召回,再大就要靠混合检索(BM25 + 稠密)打破,这是给后面埋的伏笔。最后给个规模感:HNSW 在 1000 万向量上能到亚 100ms;存储上维度越高越贵,float32 下 384 维约 1.5GB/百万向量,1024 维约 4GB/百万向量——所以选型本质是在"够用"和"烧钱"之间找平衡。

有一个被反复低估的事实:**检索质量是 RAG 的天花板。** 生成器只能回答检索器给它的东西。AWS 的人说得很直白:"A RAG is only as good as its retriever"——检索没捞到,后面再好的模型也白搭。所以调 RAG 的顺序永远是:先调检索,再谈生成。

## 为什么是 RAG,不是微调

既然模型缺知识,为什么不直接微调?两者解决的其实是不同的问题:

| 维度 | RAG | 微调 (Fine-tuning) |
|---|---|---|
| 更新知识 | 重新入库,分钟级 | 重新训练,小时到天级 |
| 来源可查 | 是,答案能回溯到原文 | 否,知识焊进权重 |
| 幻觉 | 大幅降低(检索错了仍会) | 可能不降反升 |
| 需要的数据 | 文档本身 | 上千条高质量标注样本 |
| 训练成本 | 零,按查询计费 | GPU 小时 |
| 适合 | 知识频繁变、要引用来源、私有数据 | 输出风格/格式/领域推理 |

两者不是对手,是互补的。业界的共识是 **RAG 管知识层,微调管行为层**:微调让模型按你的格式输出,再叠一层 RAG 让事实保持新鲜、可溯源。一个容易被忽略的点:微调反而可能加剧幻觉——知识焊进了权重,没有来源可查,错了也不知道。

## 分块:最容易被低估的一步

如果把 RAG 的质量问题归因,一半以上出在检索;而检索质量的一半,出在分块。分块错了,后面没有东西能补救——embedding 模型、reranker、LLM 都只能放大这个错误,不能修复。

核心是一个"金发姑娘"权衡:

- **太小**(比如不到 100 token):每一段都是一句话的碎片。检索精确,但捞出来没有上下文——代词指代谁?实体是谁?答案散落在好几段里。
- **太大**(超过 1000 token):每一段都是一个"话题的平均值"。相似度信号被稀释,不相关的内容塞满上下文窗口。

这个权衡是可测量的,不是玄学。一篇 2025 年的多数据集研究:SQuAD 的 recall@1 在 **64 token** 时最高(64.1%),涨到 512 token 掉了 10～15 个百分点——事实型答案就藏在一句话里,块越大噪声越多。而 TechQA 反过来,从 128 token 的 16.5% 涨到 512 token 的 61.3%——它的答案是描述性的、分散在多句里。同一个设置,两个数据集,最优块大小完全相反。所以不存在通用最优解,**分块是一个针对语料调的超参数**。

### 七种分块策略

| 策略 | 怎么做 | 适合 | 坑 |
|---|---|---|---|
| 固定长度 + 重叠 | 按 N token 滑窗切,留 10～15% 重叠 | 结构均匀的文本、快速原型 | 句子从中间切断、表行切碎 |
| 递归 / 结构化 | 按段落→句子→字符逐级切;Markdown 按标题切 | 技术文档、Markdown、代码 | 超大段落仍会被硬切 |
| 句子窗口 | 只嵌入单句,检索命中后扩成前后 N 句再喂 LLM | 新闻、报告、书籍 | 单句太短,丢代词和限定 |
| 语义分块 | 先算每句的 embedding,相似度骤降处断开 | 话题频繁切换的语料 | 入库多一次推理,未必比固定长度好 |
| Late Chunking | 先对整个文档做一次长上下文嵌入,过完 transformer 再分块 | 长文档、跨段引用多 | 需要支持长上下文的嵌入模型 |
| 父子分块 | 小的子块用于检索,命中了回取大的父块喂给 LLM | 长结构化文档(合同/手册) | 上下文膨胀 30～60% |
| 上下文增强 | 入库时让 LLM 给每段写一句"这段在讲什么"再嵌入 | 元数据密集的语料(日期/版本/产品号) | 入库贵,每段一次 LLM 调用 |

### 逐个看这七种策略

**固定长度 + 重叠**:按 N token 滑窗硬切,是所有人的基线——论文报告新策略,都是拿它当"打不过的对手"。赢在结构均匀的文本和快速原型。坑也直白:句子从中间切断,表行会被切碎。

**递归/结构化**:按"段落→句子→字符"的优先级逐级切,能保段落就保段落。LangChain 的 RecursiveCharacterTextSplitter 就是这个思路;Markdown 变体按标题切,把标题路径带上(比如 "## 架构 > ### 数据库选择"),正文就继承了标题里的关键词;代码变体按函数/类切,或用 tree-sitter 这种 AST。适合技术文档、Markdown、代码。坑:超大段落仍会被硬切——但它结构最多、默认最稳。

**句子窗口**:索引只嵌单个句子(检索精确),命中后把前后 N 句一起取出来喂给 LLM(上下文完整)。LlamaIndex 的 SentenceWindowNodeParser 默认 window_size=3,即命中句前后各 3 句、共 7 句。注意代价:窗口句不嵌入,只有命中句的向量参与检索——这是它"精确 + 完整"两头都要的代价。2025 年一个对比里它语义相似度 0.700,胜过 auto-merging(0.505)、multi-query(0.620)、HyDE(0.693)。适合新闻、报告、书籍。

**语义分块**:先把每句嵌入,算相邻句的相似度,相似度骤降的地方就是"话题换了",在此断块。LangChain 的 SemanticChunker 用百分位/标准差等阈值。优点:块内部话题一致、几乎不用重叠。坑:入库多一次推理(每句都要嵌入),而且 2026 年一些评测发现它对真实数据未必赢固定长度——嵌入模型的质量,往往比分块策略更重要。

**Late Chunking**(Jina,2024 年 9 月):反着来——先用支持长上下文的嵌入模型把整个文档过一遍,在 transformer 输出之后、平均池化之前再分块。这样每个块的 embedding 都被整个文档"看过":"城市"这个词在块 A 里出现、但真正指代的"柏林"在前面的块里,它的向量也能带上这个关联。Jina 的例子里,查询"柏林"的相似度从 0.753 涨到 0.850。坑:要求模型支持长上下文 + 平均池化,大多数常用嵌入模型不支持——先查你的模型支不支持,再决定要不要学它。

**父子分块**:索引小块(256～384 token)做精确检索,每块在元数据里记着父块的 ID;命中子块就回取父块(一个章节或几千字)喂给 LLM——等于把"检索精度"和"生成上下文"解耦。长结构化文档(合同、手册、法规)常用。坑:喂给 LLM 的上下文膨胀 30～60%,而且改分块要整个重新入库。

**上下文增强**(Anthropic Contextual Retrieval,2024 年 9 月):入库时让 LLM 给每段写一句 50～100 token 的"上下文定位"(这段出自哪、前文发生了什么),嵌入的是"定位 + 原文",同时把定位后的文本也喂进 BM25。Anthropic 的基准:top-20 检索失败率 5.7% → 3.7%(上下文嵌入)→ 2.9%(再加上下文 BM25)→ 1.9%(再加 rerank)。坑:每段一次 LLM 调用,入库最贵(用 prompt 缓存降到约 1 美元/百万 token);而且段落本身越自包含(比如 FAQ 问答对),收益越小。

### 参数怎么定

- **块大小**:最常被引用的起点是 **512 token**(约 50 token 重叠)。按问题类型调:事实型短问答 256～512,分析型/多跳 512～1024,整段理解 1024～2048。2026 年的生产基线是 400～600 token + reranker。
- **重叠**:传统惯例 10～20%(15% 常见),但 2026 年一篇系统分析发现重叠几乎没带来可测量的收益(BERTScore 变化 ≤0.004),却让块数膨胀约 1.25 倍。建议从 0 开始,用你自己的数据验证过再加大。
- **按 token 切,别按字符切**:切分要对齐 embedding 模型的 tokenizer,保证块不超出模型的上下文窗口被静默截断。
- **代码永远别从函数中间切**:按函数/类切,或直接用 tree-sitter 之类的 AST 分块。

### 按文档类型选

长文叙事 → 句子窗口,或递归段落切分(150～400 token);代码 → 按 AST/函数切,绝不从中间切;Markdown/HTML → 按标题切,把标题路径带上;PDF → 先 OCR 再句子窗口;长结构化文档 → 父子分块 + rerank。

没有把握就从"递归分块 + 512 token"起步,在约 50 条问题的评测集上量一下 Recall@5,再决定要不要上贵的策略。2026 年的共识:先固定 400～600 token 递归切分 + reranker,量出检索缺口,才值得为语义/late/上下文这类策略付 2～200 倍的入库成本。

## 怎么客观评价一个 RAG

搭好了,怎么知道它好不好?RAG 的每个环节(分块、嵌入、检索、生成)都能调,问题是:调完到底变好没有?靠肉眼感觉"看起来答得不错"不算数,它不可复现,还会骗你——答案更流畅了,事实可能反而更远了。这个系列一贯的态度:先量出来,再说好坏。

目前最主流的评估框架是 **RAGAS**(GitHub 约 15.1k stars,Apache-2.0,来自 explodinggradients 团队,EACL 2024)。它定义了 4 个指标,分两组:

| 组 | 指标 | 测什么 | 要不要标注 |
|---|---|---|---|
| 生成侧 | Faithfulness 忠实度 | 答案论断能否从上下文推出来 | 不用 |
| 生成侧 | Answer Relevancy 答案相关性 | 答的是不是被问的那个问题 | 不用 |
| 检索侧 | Context Precision 上下文精确度 | 相关片段排没排前面 | 要 |
| 检索侧 | Context Recall 上下文召回 | 上下文够不够全 | 要 |

faithfulness 的玩法:把答案拆成原子论断,逐个问"这条能否从检索到的上下文推出来",得分 = 被支持的论断占比——直接抓幻觉。answer relevancy 更绕:让 LLM 只根据答案反推 3 个"可能被问的问题",再和原问题算余弦相似度,答非所问会暴露。这两者的机制都是 LLM 当裁判(LLM-as-judge)。RAGAS 最大的卖点就是前两个不需要人工标注 ground truth:免标注、自动化、可规模化。搭完 RAG,拿一组固定问题跑一遍记下分数;改成配置 B,同组问题再跑一遍——faithfulness 升了没,一目了然。(注:较新版本的 RAGAS 把检索侧两个指标改名成了 Context Relevance 系列,思路一致,查文档时别对不上。)

但 LLM 裁判有个绕不开的问题:**它会继承模型自己的偏差**——冗长偏差(越长的答案分越高)、对 prompt 措辞敏感、自己不确定的地方反而心虚。最尖锐的质疑来自 Amazon 的 **RAGChecker**(NeurIPS 2024)。它做了论断级(claim-level)的元评估,发现 RAGAS 的 faithfulness 与人类整体判断的相关性很弱——Pearson 相关系数只有约 7.8,而 RAGChecker 与人类判断的整体相关性能到 61.9。通俗说:一个"看起来忠实"的 RAGAS 高分,可能和真人认为的好坏对不上。

RAGChecker 的方法论不同:不按整段答案打分,而是把答案、ground truth、检索上下文全部拆成原子论断,逐条判断"这条论断能否被上下文蕴含"。由此得到一组细粒度诊断指标:overall precision/recall/F1、claim_recall(检索捞全了没)、faithfulness、context_utilization(上下文里的有用信息用了多少)、hallucination(幻觉比例)、noise sensitivity(对噪声多敏感)。用它最大的好处是能**定位问题出在检索还是生成**——这正是它和"一个笼统总分"的本质区别。RAGChecker 还在自己的 benchmark(8 套 RAG 系统、2 个检索器 × 4 个生成器、4162 个问题、10 个领域)里验证了一个和本系列前面完全一致的观点:**检索质量是最主要的因素**——dense 检索器 E5-Mistral 一致地赢过 BM25,换什么生成器都一样。

中文场景有现成的 benchmark:评估中文 RAG 常用 **CRUD-RAG**(3.6 万个中文测试样本,按 Create/Read/Update/Delete 四类任务组织)和 **RGB**(中英双语,涵盖噪声鲁棒性、反事实鲁棒性等能力)。

最后是务实建议。没有完美的评估框架:RAGAS 便宜、免标注,适合做快速回归对比——"配置 B 比 A 好在 faithfulness 上吗",一组问题跑两遍就有数;RAGChecker 更重(每个答案要跑一次"论断抽取 + 判断"的 LLM),但能告诉你锅在检索还是生成;医疗、法律这类高风险领域,任何自动分数都要拿一小撮人工标注去校准。2026 年的共识:没有通用赢家,组合着用。最差的做法是什么都不量——"我觉得它变好了"。还记得前面那句"检索质量是天花板"吗?量化评估,就是先把这块天花板的位置量出来,再决定力气往哪花——量完调,调完再量,直到分数不再动。这也是"别让它记,让它查"能成立的底气:查得好不好,靠分数背书,不靠感觉。

## 从 Naive 到 Agentic:RAG 的五年

RAG 这个概念 2020 年由 Lewis 等人提出(《Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks》,NeurIPS 2020)。那时候模型是 BART,检索器是 DPR,索引是 Wikipedia,分块是 100 词的定长切块。它证明了"参数化记忆(BART)+ 非参数化记忆(索引)"能配合:换掉索引、模型不用重训,知识就更新了。

之后的进化按五代看:

### Naive RAG(2020～2022)

固定的"检索-然后-读"链条。问题:分块噪声、检索不精确、"lost in the middle"(上下文中间的信息被模型忽视)。

### Advanced RAG(2023)

在固定链条前后加优化:

- **检索前**:查询改写、查询分解、HyDE(让 LLM 先写一个"假设答案",拿它去检索,弥合问句和文档的措辞差距)。
- **检索中**:混合检索——BM25 稀疏向量 + dense 向量,RRF 融合。dense 抓语义,BM25 抓精确词、产品号、专有名词。
- **检索后**:cross-encoder rerank、上下文压缩。先捞 top-20～50,重排后留 top-3～5。

### Modular RAG(2023～2024)

把 RAG 拆成可插拔模块(索引、预检索、检索、后处理、记忆、生成、编排),可以线性、条件、并行、循环地组合。查询可以路由:走 RAG、走联网搜索、还是走 SQL。

### Graph RAG(2024)

微软的 **GraphRAG** 用 LLM 把语料抽成知识图谱,社区检测后给每个社区写摘要,能回答"整个语料的全局性问题"——普通 RAG 做不到。代价是入库成本爆炸(大型语料早期要几万美元)。**LightRAG** 用廉价的实体/关系图把它拉回实用,**LazyGraphRAG** 把索引成本压到与普通 RAG 一个量级。

同一时期还有 **RAPTOR**——把块递归地聚成树,每一层用 LLM 把一组相似块摘要成父节点,检索时按问题需要的粒度选层——和 GraphRAG 同属"超越平铺 top-k"的路线,适合整体性、跨文档的问题。

### Agentic RAG(2024～2026,当下)

检索从"流水线的一步"变成"agent 循环里的一系列决策":

- **什么时候检索**——Self-RAG 让模型自己判断要不要查;Adaptive-RAG 按问题难度路由(简单问题直接答,不查)。
- **检索什么**——改写问题、拆成子问题做多跳检索。
- **信不信检索结果**——Corrective RAG 给检索结果打分,不行就转联网搜索或重查。
- **用哪个工具**——向量库、web 搜索、SQL、代码解释器并列,模型自己选。

工程上,这个模式收敛成"快速路由器 + 小规划器 + 有界循环"。LangGraph 这类图框架把"要不要检索""检索后要不要再检索"变成图里的条件边——和本系列 LangGraph 那篇是同一套思路。

### 绕不开的争论:长上下文是不是让 RAG 过时了

2024 年的论点是"上下文窗口都 100 万 token 了,直接全塞进去"。但实证不支持:

- **长窗口不平均**。中间位置的信息掉 30%+ 准确率(lost-in-the-middle),模型声称的窗口长度实际不可用。
- **成本是压倒性的**。RAG 每次查询通常几百 token,全量塞约 100 万 token,成本差约 **1250 倍**(未计 prompt 缓存;缓存会显著缩小这个倍数)。
- **更新跟不上**。RAG 换一批文档是分钟级,长上下文要重新喂。

到 2026 年,"长上下文取代 RAG"的论点基本被否掉,共识是混合:**用 RAG 检索,用长上下文推理**——检索捞 top 候选,长窗口负责把候选读透。

### 2026 年的现状

- **Agentic RAG 是默认形态**:检索是 agent 的一次工具调用,不是 prompt 模板。
- **混合检索是共识架构**:2026 年 Q1 混合检索采用率从 10.3% 涨到 33.3%;rerank 被视为必须——"top-20 + rerank 到 3"打败任何单一检索器的"top-3"。
- **多模态检索落地**:ColPali 把 PDF 页面当图像检索,不用 OCR。
- **MCP 成为 agent 与检索后端之间的标准接口**——呼应本系列那篇 MCP 文章。
- **"RAG" 这个标签本身在被"context architecture"取代**:agent 在运行时通过工具拉数据,向量库变成"事实的单一来源"。

一句话:RAG 没死,变体在死。固定"检索-拼 prompt"的原始架构正在退场,取而代之的是"agent 决定何时、如何检索"。

## 实测:按前面的建议,把这个博客做成知识问答

前面把原理、分块、评估、发展都讲完了,现在照方抓药,把这个博客真做成一个知识问答——不是随手切切的玩具,每一步都严格照着前面的建议来。按文档类型选那一节说得很直接:Markdown/HTML → 按标题切,把标题路径带上。这个站的语料全是 Markdown 文章,那就该让每个 \`##\` 小节成为一块,标题本身就是语义锚;所以主方案 = 按章节切,对照组 = 固定 300 字符 + 60 重叠的机械滑窗——后者不是拿来用的,是拿来证明分块到底重不重要。换句话说,这一节的出处都能在前面找到:按标题切来自"按文档类型选",对照组的意义来自"分块是针对语料调的超参数",点评的标准来自"检索质量是天花板"。

技术栈很朴素:本地 CPU 跑 bge-small-zh-v1.5(512 维)做嵌入,FAISS IndexFlatIP 存向量,DeepSeek 生成答案——就是前面"工作逻辑"那两条流水线,离线入库一次,在线问答每次检索。语料是这个站自己的 10 篇文章,约 5 万字符。整个系统一屏放得下:

\`\`\`python
from sentence_transformers import SentenceTransformer
import faiss, numpy as np

model = SentenceTransformer("BAAI/bge-small-zh-v1.5")   # 本地嵌入,512 维
index = faiss.IndexFlatIP(512)

# 按前面"Markdown 按标题切"的建议:每个 "##" 小节是一块
chunks = [c for a in articles for c in split_by_section(a)]   # 10 篇 → 59 段
index.add(np.asarray(model.encode(chunks, normalize_embeddings=True), dtype="float32"))

q = np.asarray(model.encode([query], normalize_embeddings=True), dtype="float32")
scores, hits = index.search(q, k=3)   # 结果就是下面的表格
\`\`\`

同一批文档,两种切法,块数和结构完全不同:

| 策略 | 块数 | 说明 |
|---|---|---|
| 按章节切(每个 \`##\` 小节一块) | 59 | 主方案,标题做语义锚 |
| 固定 300 字符 + 60 重叠 | 189 | 对照组,最朴素的机械切法 |

先问一个必须跨两篇文章才能答的问题:"具身智能三代方法的反馈来源分别是什么?"——它需要同时捞到 embodied-ai(RL/模仿学习)和 vla(VLA)两篇。按章节切 top-3:

| 相似度 | 来源 | 片段开头 |
|---|---|---|
| 0.647 | embodied-ai · 结论 | 两代方法解决同一个问题,路径不同:RL 靠环境反馈自我改进,模仿学习靠数据直接告诉模型答案… |
| 0.618 | vla · 结论 | 三代方法的反馈来源一路在变:RL 用环境奖励、模仿学习用专家演示、VLA 用"演示 + 语言指令"… |
| 0.520 | embodied-ai · 为什么是扩散 | 因为动作分布是多模态的。推一个 T 形方块,有多种"都对"的推法… |

第二个命中的 vla · 结论,恰好就是答案本身。同一批语料换固定滑窗,top-3 就全来自 embodied-ai(0.632 / 0.623 / 0.571),根本没捞到 vla——它只能答出"两代",答不出"三代"。分块切对了,跨文章的答案才拼得齐。

但反过来也成立:**没有一种分块策略全赢**。同一个语料,问"Diffusion Policy 和 flow matching 是什么关系",按章节切 top-3 全部命中 langgraph——完全不相关,错误;对照组固定滑窗反而把 vla 里讲 flow matching 的段落顶到了第一位(0.727)。章节切保结构、固定切保覆盖,谁好用,得拿你自己的问题集去量——这正是前面那句"分块是针对语料调的超参数"的实锤。

最后让 DeepSeek 基于按章节检索到的上下文作答,不喂任何参考答案。它把三代归纳得很清楚:

\`\`\`
1. 强化学习(RL):反馈来源是环境奖励,通过试错学习策略。
2. 模仿学习:反馈来源是专家演示(人类演示数据),不再需要奖励函数。
3. VLA:反馈来源是"演示 + 语言指令",同一套权重靠换指令切换任务。

三代方法的反馈来源从"环境奖励"演进到"人类演示",再到"演示 + 语言指令"。
\`\`\`

答案对,因为它检索对——vla 那段被捞进来了,这就是"检索质量是天花板"在起效:检索错了,这段生成的每个字都可能是编的。按前面的方法,几分钟搭出来的不是一个玩具,是能用的知识问答——它和玩具的区别不在代码量,而在于每一步都有依据。评估那一节的思路,也正好拿来给它打分:一组固定问题、RAGAS 跑一遍,把分数当基线存下来,以后动任何一块,都有据可查。

## 结论

回到开头的问题:怎么让模型知道它没学过的东西?答案是**别让它记,让它查**。

RAG 的全部骨架是两条流水线:入库把资料变成可检索的向量,问答时把最相关的几段捞出来拼给 LLM。骨架很简单,难在细节——分块怎么切、检索用什么策略、要不要 rerank、要不要用 agent 决定检索时机。这些细节里,分块最容易被跳过,却几乎决定了检索质量。

对学习者来说,自己搭一遍是最快的路径:一段代码、一个本地嵌入模型、一个 FAISS 索引,数据就用你自己的文档。跑通了,你就同时理解了嵌入、相似度、向量库、分块这些词——它们不神秘,只是一条让模型"学会查资料"的流水线。

而 RAG 的进化方向也很清晰:从"无脑检索"到"判断该不该检索"。这跟这个系列在 agent 上看到的趋势是同一条——把确定性流程交给代码,把判断交给模型。`,
  takeaway: 'RAG 的原理是"先检索、再生成":入库把文档切成块、嵌入成向量、存进向量库;问答时把问题嵌入、捞 top-k、拼进 prompt 让 LLM 基于资料回答。骨架只有两条流水线,难在细节:分块策略直接决定检索质量,是 RAG 里最容易被低估的杠杆;检索质量是整条链路的天花板;评价分两段——检索用召回类指标,生成用 LLM 裁判(RAGAS),但裁判本身要校准,Amazon 的 RAGChecker 发现它和人类判断相关很弱;2026 年的生产基线是混合检索 + 强制 rerank + agent 决定何时检索。分块、嵌入、向量库、rerank 这些词,自己搭一遍就全懂了——一段代码加你自己的文档,就是一次完整的 RAG。',
}

export default project
